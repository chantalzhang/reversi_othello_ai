#!/bin/bash

python3 simulator.py --player_1 gpt_greedy_corners_agent --player_2 minimax_with_gpt_greedy_corners

if[ $1 == "gpt" ]; then
    python3 simulator.py --player_1 gpt_greedy_corners_agent --player_2 minimax_with_gpt_greedy_corners
fi
